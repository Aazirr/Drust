{
  "name": "drust",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "dev:worker": "npm --workspace apps/worker run dev",
    "dev:bot": "npm --workspace apps/bot run dev",
    "dev:pairing-helper": "npm --workspace apps/pairing-helper run dev",
    "build": "npm run build --workspace @drust/domain && npm run build --workspace @drust/web && npm run build --workspace @drust/worker && npm run build --workspace @drust/bot && npm run build --workspace @drust/pairing-helper",
    "lint": "npm run lint --workspace @drust/web --if-present"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
